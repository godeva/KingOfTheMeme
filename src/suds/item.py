import thing

class Item(Thing):
	def __init__(self, name, desc, NEEDS_MORE_PARAMS):
		pass #@todo(aaon) code this

class Container(Item):#an item that you can put other items in!
	def __init__(self, name, desc, inv, NEEDS_MORE_PARAMS):
		pass #@todo(aaron) design theitem system before implementing it
